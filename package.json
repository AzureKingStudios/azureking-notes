{
  "name": "azureking-notes",
  "version": "1.0.0",
  "description": "starter skeleton for a react express app",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "env-cmd nodemon index.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "azurekingstudios",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "bcryptjs": "^2.4.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.5.11"
  },
  "devDependencies": {
    "env-cmd": "^9.0.1",
    "nodemon": "^1.19.1"
  }
}
